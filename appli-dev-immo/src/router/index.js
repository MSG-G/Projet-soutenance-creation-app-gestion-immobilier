import { createRouter, createWebHistory } from 'vue-router'

//mpotation des pages de views

const routes = [
    
 //chemin des pages
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
